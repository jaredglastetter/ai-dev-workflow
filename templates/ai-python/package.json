{
  "name": "ai-python",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "pytest -q",
    "lint": "ruff check . || echo 'Install ruff: pip install ruff'"
  }
}
